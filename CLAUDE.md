# alt-kintone プロジェクトコンテキスト

**alt-kintone = 業務フローを第一級の概念に置いた、AI前提の業務アプリ基盤**（構想: `docs/product-concept.md`）。kintone の再実装ではなく、kintone が構造的に解けなかった問題を「アプリを作るのは人間ではなくAI」という前提から解く。
**最初の適用先**として、客先（求人広告・MEO営業会社）の kintone（CRM/SFA）置き換え提案を準備中。
**このファイルはセッション横断のコンテキスト共有ハブ。作業の節目で必ず「状況サマリ」「次のアクション」を更新すること。**

## 状況サマリ（最終更新: 2026-08-17、フェーズ13完了 + 道具の入れ替え）

- リポジトリ: https://github.com/okayus/alt-kintone （private）
- **2026-08-17: 開発の道具を vite-plus（beta）から入れ替えた**（`mise` / `pnpm` ネイティブ / `Biome` / `vitest` 直叩き / `TypeScript 7`）。**アプリのコードは実質そのまま**（lint の指摘6件を直しただけ・491テスト通過）。詳細は下の「開発環境」
- フェーズ: **実装（最小スコープ完了）**。構想・設計は決着済み
- **実装セッションは `docs/implementation.md` から始める**（実装ハブ）
- 現在地: **フェーズ13まで完了（2026-08-13）。業務フローが2本載り**、営業フローが動き、フローそのものも画面で読め、案件一覧は1万件をスプレッドシートライクに読み書きでき、マネージャーは全案件を閲覧のみで読め、**開発への改善要望が業務フローとして回り、その返事に「こう変わります」が業務の言葉で付き**、**案件に社内のやりとり（相談・指示・引き継ぎ）が載って制作・MEO運用が参加し**、**FEの取得は宣言（TanStack Query）になって手書きの並行制御が消え**、**やりとりは本文の隣のサイドパネルで読み書きできる**（`docs/impl/phase-*.md`）
- **FEの取得は宣言**（フェーズ12）。`@tanstack/react-query` v5 に載せ、**手書きの並行制御5種類が消えた**（`let live` 8ファイル・`requested` Set・`setInterval` 3本・再取得の合図 `generation`・PATCH 結果の差し込み）。キーは `keyOf(client, table, { user, asOf }, ...)` で作り、**先頭の `flow` は `Client.flow` から読む**。fetch のエラーは `QueryCache.onError` 1箇所が上部バナーに出す。**サーバは1行も変えていない**
- 動いているもの: `@alt/dsl`（条件式AST・テーブル定義・フロー定義・ロール定義・rowFilter・**追記の宣言 `appendBy`**・定義バンドル。**表示名 `label`・ステップの `intent`・出口条件の `howTo` は必須**、表示用の `referencedFields`、**`AST_VERSION` は 2** = 部分一致 `contains` 追加、**`definitionRef`（定義そのものへの参照）と `fill: 'createdAt'`**、**遷移グラフのレイアウト `layoutFlow`**）、`@alt/sql`（AST→SQL変換・DDL＋**外部キー索引の自動付与**・プラットフォームテーブル・有効期間型の読み書きSQL＋**窓取得**・方言）、**`@alt/diff`（定義バンドルの差分＋合併グラフ。CLIとFEが共有）**、`@alt/definitions`（**テーブル9本＋業務フロー2本**＝営業・改善要望）、`@alt/cli`（`alt validate` / `apply` / `export` / `seed` / **`diff`**。**`seed --deals N` で1万件規模**）、`@alt/server`（REST API＋**一覧の窓取得・フィルタ語彙→AST**）、`@alt/main`（エンドユーザーFE。React + Vite + **TanStack Query**。**一覧のセル編集**・**要望の起票/一覧/詳細/変更案**・**案件と要望のやりとり**込み）、適合テスト7件。**491テスト通過**（うち28本は実Chromiumのbrowserテスト）
  - **ステップの担当は `roles: string[]`（複数可）、フローは `viewers`（業務を進めない参加者）を持つ**（フェーズ8）。参加の種類は `admin`/`operator`/`viewer`/`none` の4つで、入口で1回決めて持ち回る
  - **viewer は「読むだけ」ではなくなった**（フェーズ11）。バインドが `appendBy: 'participants'` を宣言したテーブルへの**追記（create）だけ**できる。更新・遷移・手動チェックは従来どおり不可
- **営業フロー1本がブラウザで動く**。案件一覧に現在ステップと未確認件数が出て、詳細に現在地・出口条件チェックリスト・遷移ボタン・編集フォームが並ぶ。**データを直すと自動判定が勝手に充足に変わり、未充足の条件には「どうすれば充足するか」（定義の `howTo`）が出る**（＝営業の入力負担を減らす、という構想の中核が実際に働いている）。未充足でも進めるが `_flow_state.unmet_checks` に残り、`as_of` で過去が読め、担当者でなければ編集ボタンが出ない
- **フロー参照画面**（`#/flows/sales`、フェーズ5）: 遷移グラフ（前進/スキップ/差し戻しの描き分け・ノード選択で強調）＋ステップカード（意図・担当・出口条件・充足のしかた・**AST機械抽出の「見ているデータ」**）＋使うデータ表。**APIを叩かず定義だけで描く**。異動者が定義ファイルなしで業務を把握できる
- **案件一覧のグリッド**（フェーズ6、読み側）: **1万件を上下スクロールで全件辿れる**（仮想スクロール＋窓取得。窓1枚 12ms）。ステップ・担当（「自分の案件」）・商材・確度・状態・見込み月・案件名で絞れ、列見出しで並べ替えられる。**絞り込みは URL に載るのでそのまま共有できる**（nuqs）。取得の合間に誰かが更新しても行はズレない（`snapshot` で時点固定）
- **一覧のセル編集**（フェーズ7、書き側）: クリック / 矢印 / Tab で移動し **Enter で編集 → Enter で確定**（= `PATCH` 1回 = 有効期間型の1バージョン。同値は送らない）。**請求額セルに金額を入れると自動判定が変わり未確認件数がその場で動く**。確定してもスクロールと絞り込みは保たれる（ソフト世代 = 決定K）。IME 変換確定の Enter は誤発火しない。**編集に入れないセルでは理由が言葉で出る**（担当者名と「いま誰として操作しているか」まで。決定S — 設計どおりの拒否が2回不具合として報告されたため）。詳細は `docs/impl/phase-7-list-grid-edit.md` 決定K〜S
- **案件のやりとり**（フェーズ11）: 案件詳細の「活動」（＝顧客との接触。出口条件が読む）の下に「やりとり」（＝**社内**の相談・指示・引き継ぎ）。自分が右の吹き出し・Enter 送信（IME 変換確定では送らない）・8秒ポーリングの新着は**最下部にいるときだけ追随**（上を読んでいる最中は「新着 n 件 ↓」）。**受注した案件で営業が引き継ぎを書き、制作・MEO運用が読んで返せる**（それまで彼らは案件を1件も読めなかった）。表示部品は `shell/chat/` に1つで、要望のやりとりも同じ部品
- **やりとりのサイドパネル**（フェーズ13）: ページ最下部の区画をやめ、**viewport に貼り付く右のパネル**へ。本文（案件の内容・活動／要望の対象・変更案）を**見ながら書ける**。閉じれば本文が広がり、幅はハンドルのドラッグ（ダブルクリックで既定幅・Esc で閉じる）。**開閉は案件と要望で別々・幅は共通で1つ**を localStorage に持つ（URL には載せない）。**閉じてもポーリングは止めず**、取っ手に「閉じてから増えた件数」を出す（**未読ではない**。サーバに状態を持たない合図）。機構は `shell/panel/`（children を知らない）、`ChatPanel` に足したのは `visible` 1つだけ。**サーバは1行も変えていない**
  - `alt apply --recreate` → `alt export --out data/definitions.json` → `alt seed --reset [--deals 10000]` → `pnpm serve`（API: `localhost:3100`）→ `pnpm dev`（FE: `localhost:5273`）
- 未着手: Go版バックエンド、クライアントヒアリング、提案書ドラフト
- **次の候補3つ**（いずれも独立に着手できる）: §8-2 論点9（決着ステップと `deal.status` の二重管理。材料は揃っている）/ 論点7（いま誰もマスタを更新できない）/ Go版バックエンドの計画
- **要判断（残っているもの）**: 汎用の定義レジストリ+CLIを基盤として作るか、客先アプリの内部構造として実装するか（`product-concept.md` §10-1）。「客先アプリを作りながら共通部分を抽出」が現時点の推奨。※ フェーズ3で `employee` テーブルと `admin` ロールの名前がサーバの定数になっており（§8-2 論点13）、この判断に触れる箇所が1つ増えた

## 客先プロファイル

- 業種: 求人広告・飲食店向けMEO対策などのIT商材を販売する営業会社
- kintone用途: CRM/SFA（商談記録など）。「フィットしていない」という課題感あり
- 特性: 社内利用のみ・低トラフィック
- **契約ユーザー数・プラン・プラグイン利用・kintoneの他業務利用は未確認**（ヒアリング項目: `docs/domain-research.md` §5）

## ドキュメント索引

| ファイル | 内容 |
|---|---|
| **`docs/implementation.md`** | **実装ハブ。実装セッションはここから始める**。現在地・実装中ずっと効く決定10項目・ドキュメントマップ。詳細は `docs/impl/phase-*.md` に分けてあり、着手するフェーズだけ読む |
| `docs/code-reading-guide.md` | **コード読解ガイド**。「ドメインモデルが型・ADT・制約・テストで意味として表現されているか」の観点で、どのファイルをどの順序でどんな仮説をもって読むか。ステージ0〜7 ＋ 制約を破って止め手を確かめる「反証の実験」10件 |
| `docs/product-concept.md` | **プロダクト構想**（何を作るか。業務フロー×テーブルのバインディング、3コンポーネント、CLI×AI開発モデル、非目標、未確定の設計判断） |
| `docs/domain-research.md` | ドメイン調査（kintone料金・業界構造・内製化論点・代替SaaS比較、出典付き、2026-06-12） |
| `docs/cost-simulation.md` | コスト試算シミュレーション（kintone vs 内製 vs Zoho、損益分岐、料金モデル比較） |
| `docs/domain-model.md` | **ドメインモデル v2**（テーブル定義・業務フロー定義3本・ロール・KPIマッピング・v1スコープ）。alt-kintoneの定義に落とす前提 |
| `docs/condition-ast.md` | **条件式AST仕様**（TSとGoの契約）。出口条件と行レベル認可の両方で使う。ノード定義・暗黙結合ルール・SQL変換・テストケース形式 |
| `docs/sales-domain.md` | 営業ドメインの一般論（4層構造・案件の設計原理・時間軸・KPI逆算・営業スタイル別の変形・Salesforce批評・客先当てはめ）。`domain-model.md` の上流にあたる語彙と原理 |

## 確定した決定事項

### プロダクト構想（`docs/product-concept.md`）

- **業務フローが第一級の概念**。テーブルは業務フローにバインドされて初めて利用できる（未バインドのテーブルはAPIが生えない）
- **バインディングは実体**。`role`（primary/reference/master）と `purpose`（何のために使うか）を持つ
  - **使用テーブルと `access` はステップの reads/writes から導出**。二重に書かせない（書き漏れ・矛盾が構造的に起きない）
- 従業員マスターのような横断テーブルは **`global: true` 宣言 + 実参照の自動記録**（§3-4 の C）。上の導出の副産物で実装コストがほぼゼロ
- **定義は TypeScript DSL**（apply時にJSONへ変換）。理由は validate の構文・参照整合を型システムが担うため。**FEが定義の型を import すれば乖離もコンパイルエラーで落ちる**（`alt generate types` は不要になった）
- **出口条件の条件式はレベル3（集計・比較まで）**。ただし**SQLに変換できる範囲**に限る（一覧での一括評価がN+1にならないため）。任意コードは書けない
- コンポーネントは3つ: エンドユーザーFE / 管理画面FE / バックエンド
- **提供対象は客先1社のみ。マルチテナントは考慮しない**（テナント分離・テナント別設定・課金の設計は不要）
- **エンドユーザーFEは初回のみAIがスクラッチ生成し、以降は保守していく**（毎回再生成しない）
  - 帰結: 定義は**バックエンドに対しては正**だが、**FEに対しては初期生成の入力**に格下げされる。定義とFEの乖離が構造的に起きる
  - 対策: 再生成でも手動でもなく、**AIが定義の差分をFEに反映する**（`alt diff --since-generated`）。そのために型定義の供給・業務フロー単位のディレクトリ規約・生成時点の記録が要る
- **FEは1つのアプリに統合**。業務フローごとに分けない（分けるとkintoneのアプリ分断の再現になる）。生成は「業務フロー画面群の追加」操作
- **共通化はシェルのみ**（ナビ・認証・レイアウト・APIクライアント・型定義）。一覧・フォーム等の業務画面は共通部品に寄せない（部品の表現力が上限になるのがkintoneの失敗構造）
- **業務フロー定義はUIに「現在地表示 + 出口条件チェックリスト + 遷移制御」として現れる**。ウィザードにはしない（営業の進行は非線形）
  - 出口条件は**自動判定と手動チェックの2種類**を持つ。条件式の表現力が営業の入力負担を決める
  - **未充足でも進めるが記録に残す**。「出口条件を満たさず進めた案件の受注率」でステージ設計自体を検証できる
- **PC中心**。スマホはレスポンシブで閲覧程度、モバイル専用画面は作らない
- 横断ビュー（どのフローにも属さない画面）は**既存バインディングの再利用に限る**。シェルのダッシュボードとして書く
- **管理画面は「把握するための場所」で、作るための場所ではない**。中核はバインディングの双方向ビュー
- テーブル作成・業務フロー登録は **CLI（`alt`）を作ってAIエージェントに操作させる**。定義はコードとしてGit管理・レビュー・差分
- **定義ファイルが正**（Kubernetes / Terraform 型）。バックエンドはその投影。**命令的コマンド（`alt bind`）は持たない** — バインディングもフロー定義ファイルに書く
- **apply は検証がローカル、本番は main マージでCI**。Gitと本番が必ず一致する
- **`alt plan` が必須**。適用前に何が起きるかを出し、CIがPRにコメントする。「業務フローの変更を影響つきでレビューできる」がkintoneとの明確な差別化点
- **破壊的変更（列削除・型変更）は `--allow-destructive` なしでは失敗させる**。D1(SQLite)はALTER TABLEが限定的で、これらはテーブル再作成になる点も影響
- **`alt validate` の3層（構文 / 参照整合 / 業務ルール）がCLIの中核**。AIの自己修正ループ（書く→validate→直す）が閉じるかは validate の質で決まる。エラーメッセージはAIが読んで直せる形にする
- 全コマンドに `--json`
- **変更履歴は有効期間型（SCD Type 2）を全テーブル・全フィールドに既定適用**（定義で明示OFF可）。後付け不能なのでデフォルトONが必須
  - 「監査ログ（誰が何を変えたか）」と「時系列分析（ある時点の状態）」は別要件。kintoneは前者しか持たないので集計に使えない。有効期間型は前者の上位互換なので仕組みは1つで済む
  - **変更の文脈（どのフローのどのステップで変わったか）も記録する**。業務フローが第一級だから取れる情報で、kintoneでは原理的に不可能
  - 実装: 現在行はビューで隠す（`opportunity` = ビュー / `opportunity_all` = 実体）、更新は「前の行を閉じて INSERT」、APIに `as_of` パラメータ
  - これで `sales-domain.md` §14 の「現在値だけでは出せない3つ」のうち履歴・スナップショットが解決。残る「目標(quota)」はテーブルを作れば済む
  - **これはイベントソーシングではない**（正が「イベント列」ではなく「状態の列」。プロジェクション不要）。動的スキーマ・定義駆動と噛み合わないため採らない
  - **業務イベント（活動・契約変更・ステージ遷移）はドメインモデルとして別に持つ**。プラットフォームの履歴は業務モデルが取りこぼした変化を拾う網であって、そこから業務イベントを推測するものではない
- **認可は業務フロー定義から導出する**。権限設定を別に書かない（kintoneは権限設定が別画面で業務と乖離する）
  - 2層: **フロー参加**（バインディングの access の範囲で読み書き）+ **ステップ操作**（ステージを進める等）
  - ロールは定義で宣言（型になる）、ユーザーへの割当はデータ（従業員マスタ）
  - **行レベル: 読みは全員、書きは担当者＋管理者**。バインディングの `rowFilter` に条件式DSLを再利用
  - **列レベルはv1では持たない**（認可は履歴と違って後付けできる）
  - **FEに認可ロジックを複製しない**。APIがレコードごとに `_permissions` を返す
  - **認可はプロトタイプの最初から実装する**（後付けが高くつく。導出するので実装量は小さい）
- **認証は外部IdPに委譲（OIDC優先、SAMLは客先IdPが対応しない場合のみ）。自前でパスワードを持たない**
  - **プロトタイプでは実装しない**。認証と認可の境界をモジュールとして切り、プロトタイプは開発用ユーザー詐称（`X-Dev-User`）で代替する
  - 開発用の詐称は**ビルド時に落とす**（環境変数切り替えにすると本番に残るリスク）
  - ヒアリング必要: 客先のIdP（Google Workspace / Microsoft 365 / 他）、SSOが使えるプランか、入退社時のアカウント管理
- **作らないもの**: DnDレイアウトエディタ、フォームビルダー、プラグイン機構、kintoneの機能パリティ
- **「非エンジニアが自分で作れる」を価値としない**。kintoneの中核価値をあえて捨て、表現力・コード管理・費用構造を取る
- **バックエンドは最終的に Go**。定義（TS DSL）・CLI・FE は TypeScript
  - Go習熟が浅いため、**TSでドメインモデル・制約を型とユニットテストで書き、それを仕様としてGoへ移す**
  - **TS版は仕様であり、Go版完成後は実装を捨てる**（言語非依存のテストケースだけ資産として残す）。TS実装に投資しすぎないこと
  - 移植の穴を塞ぐため、**テストケースはJSONで持ち、TS版とGo版が同じケースを流す**
  - **条件式のAST（JSON）がTSとGoの契約**。仕様として最初に固める部分
  - ⚠ **Cloudflare Workers + D1 は使えない**（WorkersはGo非対応、D1はWorkers専用）→ 提案フェーズまでに `docs/cost-simulation.md` の書き換えが必要。代替は Cloud Run + Neon/Supabase など
- **インフラは今は決めない。プロトタイプはローカルで動けばよい**（SQLiteで足りる）。**優先はドメインモデリングの実装**
  - ⚠ 条件式のSQL変換は**方言依存**。ローカルSQLite → 本番PostgreSQL で書き直しになるので、**SQL生成層は差し替え可能な形にしておく**

### 提案・ドメイン

- 提案の論拠は「コスト削減」単独にしない。安価SaaS（Zoho 1,680円/人）が存在するため、**業界特化のフィット（ヨミ管理・原稿進捗・ストック契約/解約管理）＋ユーザー数非依存の費用構造**をセットで主張する
- 金額表記は**税抜・円**で統一
- ドメインモデルは「商談（deal）」と「契約（contract）」を分離し、契約は求人広告（フロー型）/ MEO（ストック型）の商材別詳細を持つ
- 営業ドメインの設計指針（`docs/sales-domain.md`、domain-model.md 改訂で反映する）:
  - 案件の金額は**顧客請求額（総額）と自社収益（粗利）を両方持つ**。代理店ビジネスで乖離するため。予測・目標は粗利ベース。一時金と月額も分離
  - 案件の**変更履歴（金額・見込み月・status・確度）を必ず持つ**。予測精度・期ずれ・パイプライン増減は後から追加できない
  - **目標（quota）エンティティを持つ**。ダッシュボードの主役は目標達成率
  - 決着区分に「消滅（No Decision）」「保留」を独立させる。保留は予測から除外
  - ステージは売り手の作業でなく**買い手の状態変化**で定義する

## 未確認事項（ヒアリング待ち・試算やモデルに影響）

1. 契約ユーザー数・プラン・年額実額（10人未満なら2026年11月の経過措置終了＝実質値上げが提案フック）
2. 有料プラグイン利用の有無と費用（コスト試算の前提P）
3. **CRM/SFA以外のkintone利用業務**（残ると解約できず削減効果が消える。提案成否の最重要確認）
4. リード（テレアポリスト）管理をスコープに含めるか
5. 顧客の粒度（企業/店舗の分離が必要か＝チェーン店顧客の有無）
6. MEO月次レポートのデータ源（手入力か、順位計測ツールのAPIか）
7. 「フィットしない」の具体箇所、増員計画、データ量・添付ファイル利用状況
8. **案件の金額を何で管理しているか**（広告費総額 or 自社マージン。目標も同じ基準か）
9. **目標（予算）の設定単位**（個人別/チーム別、月次/四半期、商材別か）
10. テレアポ担当と営業担当が分業か（分業なら引き渡しの記録が必要）
11. ヨミの段階定義とヨミ会の運用、期ずれの扱い
12. **客先のIDプロバイダ**（Google Workspace / Microsoft 365 / 他）、SSOが使える契約プランか、入退社時のアカウント管理の運用 ← 認証をIdPに委譲するため
※ 8〜11 を含む追加ヒアリング項目の全体は `docs/sales-domain.md` §17-3

## 開発環境（2026-08-04 構築）

`../fullstack-monorepo` を参考にした Docker + TypeScript 構成。詳細は `README.md`。

- **pnpm workspace**（`packages/*`, `apps/*`）+ TypeScript 7.0.2 + `tsconfig.base.json`（strict, NodeNext）
- **道具（2026-08-17 に vite-plus から移行）**: **mise**（Node と pnpm の版。`mise.toml` が唯一の真実。`packageManager` は持たない・corepack も使わない）/ **Biome**（整形 + lint。`biome.jsonc`）/ **vitest**（`vitest.shared.ts`）/ **vite 8**（FE の dev・build）/ **tsx**（`alt`・`serve` の実行）。vite-plus が beta だったのが理由で、**入れ替えたのは道具だけで、コードの意味は変えていない**
  - **`packages/*` は `dist` を作らない**（`main`/`types`/`exports` も持たない）。参照は paths / alias / `tsx --tsconfig` がソースを直接指す。**配線を書き漏らすとその場で `ERR_MODULE_NOT_FOUND`** になる（以前は `dist` の古い成果物が静かに読まれていた）
  - **`biome.jsonc` は `.json` にしない** — Biome は `biome.json` 中の `//` コメントを**エラーにせず黙って既定設定へフォールバックする**（設定が効いていないことに気づけない）
  - **Biome の recommended から4つ落としてある**（`useLiteralKeys` 235件 / `useExhaustiveDependencies`（決定Kの `resetKey` を壊す誤検知）/ `noNonNullAssertion` / a11y 15件）。理由は `biome.jsonc` に書いてある。**import の並べ替え（assist）は切ってある** — 入れると移行と無関係な84ファイルが並び替わるため、要るときに `"on"` + `pnpm fmt` 1回で足りる
  - **`mise.toml` を書き換えたら `docker compose build`**（版はイメージのビルド時に固定される）。⚠ `/app/mise.toml` が信頼されていないと**エラーを出さずにイメージ側の Node へ黙って落ちる**（`Dockerfile` の `MISE_TRUSTED_CONFIG_PATHS`）
- **`apps/main` のテストは2層**（2026-08-08）: unit（node・純関数）+ **browser（vitest browser mode、実 Chromium）**。`*.browser.test.tsx` が該当。Chromium は**イメージに apt で焼いてある**（playwright にDLさせない。`CHROMIUM_PATH` で差し替え可）。グリッドのキーボード配線のような「DOMフォーカスの所在が本体」の挙動はこの層に書く（README「ブラウザテスト」）
- パッケージ: `@alt/dsl`（条件式AST・テーブル定義・フロー定義・ロール定義・定義バンドル・遷移グラフのレイアウト）、`@alt/sql`（AST→SQL変換・DDL。**Goに移植する部分**）、`@alt/diff`（定義バンドルの差分・合併グラフ。**CLIとFEが共有し、Goには移植しない**）、`@alt/definitions`（客先の定義そのもの。テーブル・営業フロー・ロール）、`@alt/cli`（`alt` コマンド）、`@alt/server`（REST API）、`@alt/main`（`apps/main`。エンドユーザーFE。React + Vite + **TanStack Query v5**。取得のキー規約は `shell/query.ts`）
- **`alt` の起動は `pnpm alt <cmd>`**（`tsx --tsconfig packages/cli/tsconfig.json` 経由）。**`--tsconfig` は必須** — 落とすと tsx が `paths` を見ず、`main` を持たない `@alt/*` を解決できずに起動しない（vitest の alias と同じ経路）
- **作業はコンテナ内**: `docker compose up -d` → **`docker compose exec dev pnpm verify`**（check:wiring → lint → typecheck → test をまとめて実行）
  - `check:wiring` は**パッケージ追加時の「4箇所」の追記漏れ**を機械検知する（下記）。`lint`（= `biome ci`）は整形の検査も兼ねる（全体で 50ms なので、以前の `fmt:check` を前に出す理由は消えた）
  - フェーズの着手/完了は `/phase-start <N>` / `/phase-done <N>`（`docs/implementation.md` 参照）
  - `pnpm install` は compose の command が起動時に実行する
  - **ポートはホスト側でずらしてある**: API = `3100`（`pnpm serve`）、FE dev サーバー = `5273`（`pnpm dev`）。ホストの3000/5173は別プロジェクトのコンテナが使用中のため。**API と FE は別々に `exec -d` で上げる**（落ちたときにどちらが死んだか分かるように）
  - ⚠ **`alt apply --recreate` / `seed --reset` の後はサーバを上げ直す**（フェーズ13 で踏んだ）。上げっぱなしのサーバは消えた inode を掴んだままで、書き込みが `attempt to write a readonly database` で落ちる。**読みは通るので書くまで気づかない**（定義を足したときの「上げ直し」と同じ対処で、こちらは**データを作り直したとき**）
  - ⚠ **コンテナを作り直すと `node_modules` が空になることがある**（匿名ボリュームだけ新しくなっても pnpm が「Already up to date」と言って再リンクしない）。`ERR_MODULE_NOT_FOUND` が出たら `docker compose exec dev sh -c 'rm -f node_modules/.pnpm-workspace-state-v1.json node_modules/.package-map.json && pnpm install'`
  - **パッケージを追加したら `docker-compose.yml` の匿名ボリュームにも追記する**
- **パッケージを追加したら4箇所**（`pnpm check:wiring` が全部を検知して落とす。手で覚えなくてよい）:
  1. `docker-compose.yml` の匿名ボリューム
  2. そのパッケージの `tsconfig.json` の `paths`（tsc用）
  3. ルートの **`vitest.shared.ts`** の `resolve.alias`（vitest・dev サーバー用）。**最寄りの設定が読まれる**ので、`apps/*` のように自前の `vite.config.ts` を持つパッケージはルートではなく**そちら**に書く
  4. ルート `package.json` の `tsx` 起動に `--tsconfig`
  - 2〜4 はすべて「`@alt/*` を解決できない」という同じ壊れ方をする。**`dist` を捨てたので静かには壊れない**（以前は古い成果物を読んで緑になっていた）
- **`package.json` / `pnpm-workspace.yaml` を編集したら `pnpm install` は自動で走る**（PostToolUse hook、`.claude/hooks/pnpm-install-on-manifest-change.sh`）。手で叩く必要はない。発火の証跡は `.claude/.hook-log`。**`mise.toml` は対象外**（版を変えたら install ではなくイメージの再ビルドが要る）
- **コンテナ内で使い捨てスクリプトを実行するとき**（DBの中身を見る等）: **Write ツールでリポジトリ内の「依存を解決できるパッケージ配下」に置き**（例: `packages/cli/check.tmp.ts`）、`docker compose exec` で実行して消す。リポジトリは bind mount されているのでホストから置ける
  - scratchpad はコンテナから見えない。`/app` 直下や `/app/data` は pnpm の strict レイアウトのため依存を解決できず `ERR_MODULE_NOT_FOUND` になる（`better-sqlite3` は `packages/cli/node_modules` にしかない）
  - heredoc で `docker compose exec sh -c` に流し込まない（クォートが壊れる）
  - `git` はホスト側で叩く（コンテナ内からは「Not a git repository」になる）

## 作業ルール

- ドキュメント・コミュニケーションは日本語
- 価格・外部情報は調査日を明記（情報源は `docs/domain-research.md` の出典に追記）
- 仮置きの数値は「仮置き」と明記し、ヒアリング後に確定値へ置換する
- このCLAUDE.mdには要約と決定事項のみ。詳細は docs/ 配下へ

## 次のアクション

**方針: 優先はドメインモデリングの実装。インフラは考えない（プロトタイプはローカル + SQLite）。**

構想の設計判断は `product-concept.md` §8-1 でほぼ確定済み（未確定は §8-2 の11件で、いずれも実装しながら決まる性質のもの）。

### 完了

- `docs/domain-model.md` の改訂（v2）、条件式AST仕様（`docs/condition-ast.md`）
- 開発環境の構築（Docker + pnpm workspace + vite-plus）
- **条件式ASTの実装**（`@alt/dsl`）、テーブル定義と外部キー解決、**SQL変換**（`@alt/sql`）、実SQLiteで動く適合テスト6件
- **フェーズ1: 定義層**（2026-08-06）— プラットフォームテーブルのDDL（`_flow_state` / `_manual_check`）、フロー定義DSL、`@alt/definitions`（テーブル5本・営業フロー1本・出口条件7件）
- **フェーズ2: CLI**（2026-08-06）— `@alt/cli`。`alt validate`（3層19ルール、エラーは論理キー＋直し方つき）・`alt apply`（SQLiteにスキーマ、`--recreate` 必須の破壊的変更ガード）・`alt export`（定義バンドルをJSON）
- **フェーズ3: バックエンド**（2026-08-06）— `@alt/server`（`node:http`、フレームワーク無し）。定義レジストリ・ルート自動生成（未バインドは404）・出口条件の一括評価（一覧のクエリは件数によらず3本）・有効期間型の書き込み（閉じてINSERT、`as_of`）・ステップ遷移（未充足でも進めて `_flow_state.unmet_checks` に記録、管理者の強制遷移）・認可4層と `_permissions`・`X-Dev-User` 詐称。`alt seed` と `alt export --out` も追加
- **フェーズ4: FE + 動作確認**（2026-08-06）— `apps/main`（`@alt/main`。React + Vite、ルータ・状態管理・CSSフレームワーク無し）。共通シェル（ナビ・ハッシュルータ・APIクライアント・時点指定・開発用ユーザー切替）+ 営業フローの画面（案件一覧 / 詳細）。**ステップ名と順序は `@alt/definitions` を値として import**（乖離しない）。完了条件6件をブラウザで検証済み
- **フェーズ5: 業務フローの参照画面**（2026-08-06）— 定義に**表示名（必須）・ステップの意図 `intent`・出口条件の充足のしかた `howTo`** を持たせ（書き忘れは型エラー）、enum を `{key, label}` の配列に。`#/flows/sales` に参照画面（遷移グラフ＝後退辺DFS検出＋最長経路法の自前レイアウト・ステップカード・使うデータ表。**APIを叩かない**）。案件詳細の未充足チェックに howTo、**「見ているデータ」を AST から機械抽出**して手書き howTo のズレを目視可能に。`labels.ts` の enum 表と `DealForm` のラベル手書きが消えた（**論点14解決**）。完了条件8件を検証済み
- **フェーズ6: 一覧グリッドの読み側**（2026-08-08）— 案件一覧を1万件規模のグリッドに。サーバは窓取得（`offset` / `total` / `now` / `snapshot` / `sort` / フィルタ語彙→条件式AST）、FE は仮想スクロール（`@tanstack/react-virtual`）+ フィルタ面 + URL 状態（nuqs）。**AST に部分一致 `contains` を追加**（`AST_VERSION` 2）。**実測が計画を1つひっくり返した** — 出口条件の相関サブクエリが窓の外まで評価され `offset=9900` が13.8秒 → **2段階取得**（id を決める→本体を引く）で12ms、**外部キー索引の自動付与**でさらに 145ms→9.5ms。**`snapshot` を `as_of` と分けた**（時点固定で一覧が読み取り専用になるとフェーズ7が成立しないため）。完了条件8件を検証済み
- **フェーズ7: 一覧グリッドの書き側**（2026-08-08）— セル編集。クリック / 矢印 / Tab 移動 + **Enter で編集 → Enter で確定 = `PATCH` 1回 = 版**（同値は送らない）。型別エディタ（text / integer / enum / yearMonth。候補・ラベル・必須は定義から）。**請求額2列を追加**（金額系自動判定が読むのは Billing。決定L）、詳細への導線は先頭「開く」列へ（決定M）。**世代をソフト/ハードに分け**、確定でスクロールが飛ばない（決定K）。IME は `isComposing`/`keyCode 229` でガード。実機で欠陥2件が発覚し対処: **「枠は出ているのにキーが届かない」**（決定R = 枠が見えている＝キーが効く、を不変条件に）と**「設計どおりの編集不可が2回バグとして報告された」**（決定S = 入れない理由を担当者名つきでその場に表示）。**vitest browser mode（実 Chromium）を導入**し、この2件を含む6本を回帰テスト化（unit と合わせ 347 テスト）。サーバ変更ゼロ・新依存はテスト用のみ。完了条件8件を検証済み
- **フェーズ8: ロールと参加の認可**（2026-08-09）— §8-2 論点12 の解決。**論点12 は2つの別要求を1つの番号に畳んでいた**: (a)「操作しないが見る」＝ `flow.viewers`（読み取り専用の参加者）、(b)「**同じ段階を複数のロールが操作する**」＝ `step.role` → `roles: string[]`。**(b) は `viewers` では解けない**（読みを通しても `advance` が現在ステップの担当ロールを要求する）。参加の種類を `admin`/`operator`/`viewer`/`none` の値にして入口で1回決め、`RequestContext` に載せる（viewer を `access` に落として表現しない＝層2の設計を崩さない）。**`POST` の穴を発見して塞いだ** — 新規作成には行レベル認可が効かない（行がまだ無い）ので、`requireOperator` が唯一の防波堤。「全ロール」は `ROLE_KEYS` の導出で書く（DSL に特殊値を作らない）。`sales_manager` の説明文が「閲覧・編集」と実態に反していたので実態に合わせた。「403 になる人」の役は鈴木から森（制作担当）へ移した。完了条件9件を検証済み（361テスト）
- **フェーズ9: 改善要望・障害報告の受付**（2026-08-09）— **2本目の業務フローが載った**。開発への要望そのものを `request` フロー（起票 → 受付 → 対応中 → 適用済 → 完了 / 見送り）として定義に書く**自己適用**。スクショに赤ペンの運用を「定義の語彙で対象を指す」形に置き換えた。基盤に足したのは2つだけ — **`definitionRef`**（定義そのものを指すフィールド。`reference()` と同型で `FieldType` は増やさない。値は `sales.proposed.timing_confirmed` の合成キー。解決はサーバとFEが同じ純関数を共有）と **`fill: 'createdAt'`**（`_version.validFrom` は更新のたびに動くので作成時刻に使えない）。**決定的だったのは定義を足しただけで `#/flows/request` の参照画面が増えたこと**（FE を1行も書いていない）。`status` 列は持たせず状態はステップだけ（§8-2 論点9 を繰り返さない）。業務フロー描画（現在地・チェックリスト・遷移）は `shell/flow/` へ移した（**一覧とフォームは移さない**）。**「フローが1本」に寄りかかっていた箇所が4つ露出**（未読の数え直し / 画面をまたぐエラー / マスタの引き先 / `roles: ROLE_KEYS` の型）。完了条件10件を検証済み（401テスト）

- **フェーズ10: 定義の差分**（2026-08-09）— **要望への返事に「こう変わります」が付いた**。`alt diff` が適用済み（`data/definitions.json`）と作業ツリーを比べ、**業務の言葉で**出す（「出る条件が増えます: 「競合先を確認した」（自動判定）」）。`--request <id>` で要望に変更案として添えると起票者が読める。遷移は**前後を重ねた合併グラフ1枚**（位置は変更後の定義で決めるので**残るノードが動かない**）。**「いま提案にいる 2000 件が全部未充足になります」まで適用前に出る**（kintone に原理的に出せない = `alt plan` の業務側の対）。**版を積む仕組みは要らなかった**（§8-2 論点5 解決）。⚠ **保存する計算結果はラベル解決済みで持つ** — 提案差分は適用したら再計算しても出ないので、機械キーだけ保存すると何も表示できなくなる（`definitionRef` とは時制が逆）。⚠ **業務の言葉の約束は「できませんでした」の文で先に破れた**（数えられない理由だけ機械名に落ちていた）。`layoutFlow` を FE から `@alt/dsl` へ移設。完了条件8件を検証済み（440テスト）

- **フェーズ12: 取得の宣言化**（2026-08-10）— §8-2 論点20 の解決。`apps/main` を TanStack Query v5 に移し、**手書きの並行制御5種類が全部消えた**（`let live` 8ファイル・`requested` Set・`setInterval` 3本・`generation` state・`replaceRow`）。**サーバは1行も変えていない**（`packages/*` の差分ゼロ）。実装して分かったことが4つ: (a) **Provider を張る関数は自分のクエリを持てない**ので `App` を2層に割った（`QueryCache.onError` → 上部バナーの置き場が決まる）、(b) **キー先頭の `flow` は文字列で渡すと取り違えが書ける**ので `Client` に `readonly flow` を生やして型で塞いだ、(c) **行の蓄積だけはライブラリが持てない** — キャッシュはキー単位なので、ソフト世代（`snapshot` が変わる）と通り過ぎた窓の2つを蓄積 Map が支える（決定K の本体）、(d) **Suspense / ErrorBoundary は入れないほうが良かった**（エラーの配りは境界なしで消え、境界を入れるとチャットの失敗で詳細画面ごと倒れる）。⚠ 検証中に判明: **完了条件に書いた「森で案件一覧 → 403」はもう再現しない**（フェーズ11 で森が営業フローの viewer になったため）。⚠ **セルを直すと行が一覧の先頭へ飛ぶ**のは既定の並びが「更新が新しい順」だからで、取得の不具合ではない（下のインデックスは動かないので決定K は壊れない）。完了条件8件を検証済み（460テスト）

- **フェーズ11: チャット**（2026-08-10）— **案件に社内のやりとりが載った**。案件詳細の「活動」（顧客との接触。出口条件が読む）の下に「やりとり」（社内の相談・指示・**引き継ぎ**）。**受注した案件で営業が引き継ぎを書き、制作・MEO運用が読んで返せる**（それまで彼らは案件を1件も読めなかった）。基盤に足したのは**宣言1つ** — バインドの **`appendBy: 'participants'`** で、**「viewer は読むだけ」を人の側ではなくテーブルの側で1点だけ緩めた**（開くのは create のみ。更新・遷移・手動チェックは不可）。表示部品は `shell/chat/` に1つで要望のやりとりも描き直し（決定13 の境界が「`_flow` を描くか」→「**形が固定の機構を描くか**」に広がった）。⚠ **客先の定義から「403 になる人」が居なくなった**（5ロール全部がどこかに参加。非参加403は純関数テストだけが守る）。⚠ **`POST` は `as_of` を弾いておらず、行は書かれるのに 404 を返していた**（フェーズ8 の「POST だけ守りが1枚少ない」の2例目。入口で 400 にして解決）。⚠ **v1 は返信に気づく手段が無い**（未読は作らない判断）。完了条件14件を検証済み（453テスト）

- **フェーズ13: やりとりのサイドパネル化**（2026-08-13）— **やりとりが本文の隣に立った**。ページ最下部の 320px の区画から viewport に貼り付く右のパネルへ移り、案件の内容・活動（要望なら対象・変更案）を**見ながら**書ける。閉じれば本文が広がり、幅はドラッグで決まり、どちらもリロードを跨ぐ（**開閉は画面種別ごと・幅は共通で1つ** — 幅の好みは画面ではなくモニタのもの）。**閉じている間もポーリングは止めず**、取っ手に「閉じてから増えた件数」を出す（未読機構ではなく「新着 n 件 ↓」の同型）。基盤に足したのは `shell/panel/`（children を知らない）と `ChatPanel` の `visible` 1つだけで、**`packages/*` の差分はゼロ**。⚠ 貼り付け方は計画の sticky ではなく **fixed + シェルの余白**になった（sticky は流れの位置から `100vh` 伸びるので**スクロール0で入力欄が画面の下に切れる**）。そのため**画面がシェルのレイアウトに触る経路**が1つできた（§8-2 論点21）。⚠ **`display:none` の間は `scrollHeight` が計れない**ので「開いた瞬間に最下部へ」は明示的に扱う。⚠ **vitest の browser は既定の窓が 414px** で、幅の検証には `page.viewport` が要る。完了条件11件を検証済み（491テスト）

### 次に進めるもの

**→ `docs/implementation.md` を読む。それが実装セッションのハブ。**

フェーズ13まで完了。次の候補は3つで、いずれも独立に着手できる:

- **§8-2 論点9（材料が揃った）** — ステップを `won` に進めても `deal.status` は `open` のままで、画面に「状態 進行中」と出る。どちらを正にするか決める。※ **フェーズ9 の要望フローは `status` 列を持たせず状態をステップだけにした**ので、「ステップを正にする」向きの実例が1つできた
- **§8-2 論点7** — **いま誰もマスタを更新できない**（`company` / `contact` が reference のみ）。※ フェーズ10 で「API を通らない書き込み」が2例目になった（`alt diff --request` が業務データを書く）
- **Go版バックエンドの計画**（`condition-ast.md` §9-5）。`testdata/condition-eval/` の同じケースを Go で流すのが最初の一歩

そのほか順番に入っていないもの:
- **§8-2 論点18** — 定義がフィールドの順序を持たない。**フェーズ10 は解かずに進めた**（画面の並びが定義から出ていないので、順序差分を出しても嘘になる）。解くのは「フォームを定義から描く」ときで、そのとき画面と一緒に決める
- **§8-2 論点17（フェーズ6で判明）** — 一覧の性能は「条件式が何を読むか」で決まる。全ステップの出口条件を毎行評価しているので、ステップと条件が増えると効く
- **§4-4 の `alt diff --since-generated`**（FE を生成した時点との差分 → コードを追随させる）。今回の `alt diff`（業務に何が起きるか）とは**別の軸**で、未着手

実装で効いている決定（変えるときは影響が広い）:
- API は **REST 自動生成**（GraphQL/gRPC は動的スキーマと相性が悪い）。**グリッドの取得も REST 拡張**（2026-08-08 旧論点16決着。GraphQL 併用は再訪条件つき見送り — §8-1「一覧グリッド化の着手前に決めたもの」）
- **時点のパラメータは2つあり意味が違う**（フェーズ6）。`as_of` = 過去を見る（読み取り専用）／ `snapshot` = いまを固定して読む（読み取り専用にしない）。1つにまとめると一覧の全行が編集不可になる
- **一覧のフィルタはフィールド毎のパラメータ**（`status=open` / `title_like=看板`）。**サーバ内部で条件式ASTに変換する**ので、FE は AST を組み立てない
- **外部キーの索引は DDL が自動で付ける**（フェーズ6）。有効期間型の列と同じで、定義に書かせない
- **フロー参加は4種類**（フェーズ8）。`admin` / `operator`（どれかのステップの `roles`）/ `viewer`（`flow.viewers`。**業務を進めない**）/ `none`（403）。入口で1回決めて `RequestContext` に載せる。**viewer を `access` に落として表現しない** — access は「フローがそのテーブルをどう使うか」であって人の偉さではない
- **追記だけはバインドの宣言で開ける**（フェーズ11）。`bind(..., { appendBy: 'participants' })` のテーブルは **create だけ**参加者全員（viewer 含む）に開く。参加の4種類も「入口で1回」も崩さず、**人の側の定義を変えずにテーブル1つぶんの例外**として書ける。⚠ テーブル単位の可否なので `_permissions`（レコードごと）には出さない
- ⚠ **書き込みの守りは「行がまだ無い操作」を必ず確かめる**（フェーズ8・11）。POST は rowFilter も `_permissions` も評価できないので、既存行を前提にした守りを2回すり抜けた（viewer の作成／`as_of` 付き作成）
- 現在ステップは **`_flow_state` テーブル**（レコード×フローの *関係* として持つ。業務テーブルの列にしない ＝ kintone と同じ構造を避ける）
- 手動チェックは **`_manual_check`、明示キーで識別**（ラベルをキーにすると文言修正でチェックが外れる）
- フローの対象は **`flow({ target })` で明示**（`primary` バインド＝所有 とは別の軸）、起点は `initial`
- **enum の値は英語キー、表示ラベルも定義が持つ**（`{key, label}` の配列。フェーズ5、論点14解決）。key は識別子なので文言修正で既存データが孤児にならない
- **ラベル・`intent`・`howTo` は定義で必須**（フェーズ5）。省略可にすると必ず書かれないものが出て画面に英語キーが漏れる
- **CLI は客先定義を静的 import する**（`packages/cli/src/bundle.ts` 1箇所に閉じる）。任意パスの実行時ロードは持たない
- **定義のルールの置き場は `alt validate` 1箇所**。定義パッケージのテストには定義集合固有の前提だけ残す
- 終端ステップの出口条件は **`next` が空なら免除**（フェーズ2で決着。保留ステップには手動チェックを足した）
- **FEは定義を値として import してよい**（フェーズ4）。「定義はFEに対しては初期生成の入力」は一律ではなく、機械的に描ける部分は読み続けてよい
- **定義そのものを指すフィールドは `definitionRef`**（フェーズ9）。`reference()` と同型で `FieldType` を増やさない。値は合成キー、解決は `@alt/dsl` の純関数を**サーバとFEが共有する**
- **サーバが埋める値は `fill: 'createdAt'` の1つだけ**（フェーズ9）。⚠ `_version.validFrom` は現在バージョンの開始時刻なので**作成時刻に使えない**
- **シェルが共通化するのは「形が固定の機構」まで**（フェーズ9 決定H → フェーズ11 で言葉を広げた）。`shell/flow/`（現在地・チェックリスト・遷移）・`shell/chat/`（やりとり。**テーブル名もフィールド名も fetch も知らない**）・**`shell/panel/`（サイドパネル。children が何かを知らない。フェーズ13）**。**一覧とフォームは各フローの手書きのまま**（ここを共通部品にすると kintone の失敗構造）
  - ⚠ **機構を「持つ」のは業務側**（フェーズ13）。パネルを持つのは詳細画面ではなく `DealChat` / `RequestChat` — 閉じている間の `silent`・取っ手の件数・`visible` が**すべて同じクエリを見ている**ので配線が増えない
- **API クライアントは業務フローごとに1本**（フェーズ9）。呼び出しごとに `flow` を渡せるようにしない — 指定漏れが認可の範囲の取り違えになる
- **定義に対する純関数は `@alt/dsl`**（フェーズ10）。Go に移植しないもの（`referencedFields` / `layoutFlow`）も**契約外と注記して同じ棚に置く**。CLI・サーバ・FE のどこからでも呼べることのほうが棚の純度より効く
- **保存する計算結果は、業務の言葉を解決してから保存する**（フェーズ10）。提案差分（`change_request.proposal`）が例。⚠ `definitionRef`（機械キー保存・表示時解決）とは**時制が逆**なので混同しない — あちらは「いまの定義のどこを指すか」、こちらは「そのとき定義がどうだったか」
- **FEの取得は宣言で書く**（フェーズ12）。`useEffect` + `fetch` を新しく書かない。キーは `keyOf(client, table, { user, asOf }, ...固有)` で作り配列リテラルを手書きしない。**先頭の `flow` は `Client.flow` から読む**ので画面が指定を間違えられない（決定14 を型で守り直した形）。`user`・`asOf` がキーに入るので**利用者を切り替えた瞬間に別のクエリになる**（フェーズ9 決定N の保証がキーの同一性に移った ＝ **未読バッジに `placeholderData` を付けると即死する**）。fetch のエラーは画面が配らず `QueryCache.onError` 1箇所が上部バナーに出す（意図的に黙らせるのは `meta.silent` か queryFn 内 catch）。書き込みは `useMutation` にしていない
- **`alt diff` は差分があっても終了コード 0**（フェーズ10）。検査ではなく説明なので、`validate` と同じに見せない
- ⚠ **定義を足したらサーバを上げ直す**（`data/definitions.json` は起動時に1回読む）。古いプロセスが残っていると「テーブルの API は生えていない」になる

未決着の要検討（`product-concept.md` §8-2 論点7・9・11・13・15・17・18・19・21）:
決着ステップと `deal.status` の二重管理（論点9、判断材料は揃った）/ **一覧の性能は「条件式が何を読むか」で決まる**（論点17、フェーズ6の実測で判明）/ **いま誰もマスタを更新できない**（`company`・`contact` が reference のみ。`alt seed` は開発用の裏口であって答えではない。**フェーズ10 で「API を通らない書き込み」が2例目になった** — `alt diff --request` は業務データを書く）/ 条件式AST の構文エラーの出し方 / **プラットフォームが客先定義の名前（`employee` テーブル・`admin` ロール）を直に知っている** / **`table()` の型消去でFEのレコード型を導出できない**（論点15。§5-6 の約束が現状は成立していない。フェーズ5で同時にやらないと判断済み — 値レベルと型レベルで直交するため）/ **定義がフィールドの順序を持たない**（論点18。**フェーズ10 は解かずに進めた** — 画面の並びが定義から出ていないので順序差分は嘘になる）/ **「サーバが埋める値」を一般化するか**（論点19）/ ※ 論点20（FEのデータ取得）は**フェーズ12 で解決**。取得の重複（未読バッジと要望画面が同じテーブルを別条件で引く）だけは**意図して残してある** — 動機は通信量ではないので、キーを画面の都合で寄せて共有させない（`impl/phase-12-data-fetching.md` 論点E）/ **画面がシェルのレイアウトに触る経路が1つできた**（論点21、フェーズ13。パネルが `--side-panel-gutter` を書き `.app` が読む。**同時に開くパネルが1つだから成立している**ので、2つ目が来たら余白の持ち主を決める）

### 客先提案（プロトタイプと並行 or 後）

6. ヒアリングシートの清書（`docs/domain-research.md` §5 + `docs/sales-domain.md` §17-3 + 上記「未確認事項」ベース）
7. `docs/cost-simulation.md` の改訂（Cloudflare Workers + D1 前提が崩れたため。Go前提のホスティング費用に置換）※提案フェーズまでに
8. ヒアリング結果でコスト試算のパラメータ（U, P, D, M）を確定値に更新
9. 提案書ドラフト（構成: 現状課題 → 選択肢比較 → 内製案 → コスト → リスクと対策 → 進め方）
   ※ 提案で基盤構想をどこまで語るかは要判断（`product-concept.md` §10-2。実験台にされる懸念を与えるリスク）
