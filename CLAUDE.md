# alt-kintone プロジェクトコンテキスト

**alt-kintone = 業務フローを第一級の概念に置いた、AI前提の業務アプリ基盤**（構想: `docs/product-concept.md`）。kintone の再実装ではなく、kintone が構造的に解けなかった問題を「アプリを作るのは人間ではなくAI」という前提から解く。
**最初の適用先**として、客先（求人広告・MEO営業会社）の kintone（CRM/SFA）置き換え提案を準備中。
**このファイルはセッション横断のコンテキスト共有ハブ。作業の節目で必ず「状況サマリ」「次のアクション」を更新すること。**

## 状況サマリ（最終更新: 2026-08-06）

- リポジトリ: https://github.com/okayus/alt-kintone （private）
- フェーズ: **実装**（構想・設計は決着済み。ブラウザで動作確認できる最小スコープを構築中）
- **実装セッションは `docs/implementation.md` から始める**（実装ハブ）
- 現在地: **フェーズ3（バックエンド）完了 → フェーズ4（FE + 動作確認）が次**（全4フェーズ。`docs/impl/phase-*.md`）
- 動いているもの: `@alt/dsl`（条件式AST・テーブル定義・フロー定義・ロール定義・rowFilter・定義バンドル）、`@alt/sql`（AST→SQL変換・DDL・プラットフォームテーブル・有効期間型の読み書きSQL・方言）、`@alt/definitions`（テーブル5本＋営業フロー1本＋出口条件8件）、`@alt/cli`（`alt validate` / `apply` / `export` / `seed`）、`@alt/server`（REST API）、適合テスト6件。**232テスト通過**
- **営業フロー1本が API として動く**。一覧に現在ステップと出口条件チェックリストと `_permissions` が乗り、更新はバージョンとして積まれ `as_of` で過去が読め、担当者でなければ 403。未バインドのテーブルは 404（＝バインドの強制が実装で効いている）
  - `alt apply --recreate` → `alt export --out data/definitions.json` → `alt seed` → `pnpm serve`（ホストから `localhost:3100`）
- 未着手: **FE（フェーズ4）**、クライアントヒアリング、提案書ドラフト
- **要判断（残っているもの）**: 汎用の定義レジストリ+CLIを基盤として作るか、客先アプリの内部構造として実装するか（`product-concept.md` §10-1）。「客先アプリを作りながら共通部分を抽出」が現時点の推奨。※ フェーズ3で `employee` テーブルと `admin` ロールの名前がサーバの定数になっており（§8-2 論点13）、この判断に触れる箇所が1つ増えた

## 客先プロファイル

- 業種: 求人広告・飲食店向けMEO対策などのIT商材を販売する営業会社
- kintone用途: CRM/SFA（商談記録など）。「フィットしていない」という課題感あり
- 特性: 社内利用のみ・低トラフィック
- **契約ユーザー数・プラン・プラグイン利用・kintoneの他業務利用は未確認**（ヒアリング項目: `docs/domain-research.md` §5）

## ドキュメント索引

| ファイル | 内容 |
|---|---|
| **`docs/implementation.md`** | **実装ハブ。実装セッションはここから始める**。現在地・実装中ずっと効く決定10項目・ドキュメントマップ。詳細は `docs/impl/phase-*.md` に分けてあり、着手するフェーズだけ読む |
| `docs/product-concept.md` | **プロダクト構想**（何を作るか。業務フロー×テーブルのバインディング、3コンポーネント、CLI×AI開発モデル、非目標、未確定の設計判断） |
| `docs/domain-research.md` | ドメイン調査（kintone料金・業界構造・内製化論点・代替SaaS比較、出典付き、2026-06-12） |
| `docs/cost-simulation.md` | コスト試算シミュレーション（kintone vs 内製 vs Zoho、損益分岐、料金モデル比較） |
| `docs/domain-model.md` | **ドメインモデル v2**（テーブル定義・業務フロー定義3本・ロール・KPIマッピング・v1スコープ）。alt-kintoneの定義に落とす前提 |
| `docs/condition-ast.md` | **条件式AST仕様**（TSとGoの契約）。出口条件と行レベル認可の両方で使う。ノード定義・暗黙結合ルール・SQL変換・テストケース形式 |
| `docs/sales-domain.md` | 営業ドメインの一般論（4層構造・案件の設計原理・時間軸・KPI逆算・営業スタイル別の変形・Salesforce批評・客先当てはめ）。`domain-model.md` の上流にあたる語彙と原理 |

## 確定した決定事項

### プロダクト構想（`docs/product-concept.md`）

- **業務フローが第一級の概念**。テーブルは業務フローにバインドされて初めて利用できる（未バインドのテーブルはAPIが生えない）
- **バインディングは実体**。`role`（primary/reference/master）と `purpose`（何のために使うか）を持つ
  - **使用テーブルと `access` はステップの reads/writes から導出**。二重に書かせない（書き漏れ・矛盾が構造的に起きない）
- 従業員マスターのような横断テーブルは **`global: true` 宣言 + 実参照の自動記録**（§3-4 の C）。上の導出の副産物で実装コストがほぼゼロ
- **定義は TypeScript DSL**（apply時にJSONへ変換）。理由は validate の構文・参照整合を型システムが担うため。**FEが定義の型を import すれば乖離もコンパイルエラーで落ちる**（`alt generate types` は不要になった）
- **出口条件の条件式はレベル3（集計・比較まで）**。ただし**SQLに変換できる範囲**に限る（一覧での一括評価がN+1にならないため）。任意コードは書けない
- コンポーネントは3つ: エンドユーザーFE / 管理画面FE / バックエンド
- **提供対象は客先1社のみ。マルチテナントは考慮しない**（テナント分離・テナント別設定・課金の設計は不要）
- **エンドユーザーFEは初回のみAIがスクラッチ生成し、以降は保守していく**（毎回再生成しない）
  - 帰結: 定義は**バックエンドに対しては正**だが、**FEに対しては初期生成の入力**に格下げされる。定義とFEの乖離が構造的に起きる
  - 対策: 再生成でも手動でもなく、**AIが定義の差分をFEに反映する**（`alt diff --since-generated`）。そのために型定義の供給・業務フロー単位のディレクトリ規約・生成時点の記録が要る
- **FEは1つのアプリに統合**。業務フローごとに分けない（分けるとkintoneのアプリ分断の再現になる）。生成は「業務フロー画面群の追加」操作
- **共通化はシェルのみ**（ナビ・認証・レイアウト・APIクライアント・型定義）。一覧・フォーム等の業務画面は共通部品に寄せない（部品の表現力が上限になるのがkintoneの失敗構造）
- **業務フロー定義はUIに「現在地表示 + 出口条件チェックリスト + 遷移制御」として現れる**。ウィザードにはしない（営業の進行は非線形）
  - 出口条件は**自動判定と手動チェックの2種類**を持つ。条件式の表現力が営業の入力負担を決める
  - **未充足でも進めるが記録に残す**。「出口条件を満たさず進めた案件の受注率」でステージ設計自体を検証できる
- **PC中心**。スマホはレスポンシブで閲覧程度、モバイル専用画面は作らない
- 横断ビュー（どのフローにも属さない画面）は**既存バインディングの再利用に限る**。シェルのダッシュボードとして書く
- **管理画面は「把握するための場所」で、作るための場所ではない**。中核はバインディングの双方向ビュー
- テーブル作成・業務フロー登録は **CLI（`alt`）を作ってAIエージェントに操作させる**。定義はコードとしてGit管理・レビュー・差分
- **定義ファイルが正**（Kubernetes / Terraform 型）。バックエンドはその投影。**命令的コマンド（`alt bind`）は持たない** — バインディングもフロー定義ファイルに書く
- **apply は検証がローカル、本番は main マージでCI**。Gitと本番が必ず一致する
- **`alt plan` が必須**。適用前に何が起きるかを出し、CIがPRにコメントする。「業務フローの変更を影響つきでレビューできる」がkintoneとの明確な差別化点
- **破壊的変更（列削除・型変更）は `--allow-destructive` なしでは失敗させる**。D1(SQLite)はALTER TABLEが限定的で、これらはテーブル再作成になる点も影響
- **`alt validate` の3層（構文 / 参照整合 / 業務ルール）がCLIの中核**。AIの自己修正ループ（書く→validate→直す）が閉じるかは validate の質で決まる。エラーメッセージはAIが読んで直せる形にする
- 全コマンドに `--json`
- **変更履歴は有効期間型（SCD Type 2）を全テーブル・全フィールドに既定適用**（定義で明示OFF可）。後付け不能なのでデフォルトONが必須
  - 「監査ログ（誰が何を変えたか）」と「時系列分析（ある時点の状態）」は別要件。kintoneは前者しか持たないので集計に使えない。有効期間型は前者の上位互換なので仕組みは1つで済む
  - **変更の文脈（どのフローのどのステップで変わったか）も記録する**。業務フローが第一級だから取れる情報で、kintoneでは原理的に不可能
  - 実装: 現在行はビューで隠す（`opportunity` = ビュー / `opportunity_all` = 実体）、更新は「前の行を閉じて INSERT」、APIに `as_of` パラメータ
  - これで `sales-domain.md` §14 の「現在値だけでは出せない3つ」のうち履歴・スナップショットが解決。残る「目標(quota)」はテーブルを作れば済む
  - **これはイベントソーシングではない**（正が「イベント列」ではなく「状態の列」。プロジェクション不要）。動的スキーマ・定義駆動と噛み合わないため採らない
  - **業務イベント（活動・契約変更・ステージ遷移）はドメインモデルとして別に持つ**。プラットフォームの履歴は業務モデルが取りこぼした変化を拾う網であって、そこから業務イベントを推測するものではない
- **認可は業務フロー定義から導出する**。権限設定を別に書かない（kintoneは権限設定が別画面で業務と乖離する）
  - 2層: **フロー参加**（バインディングの access の範囲で読み書き）+ **ステップ操作**（ステージを進める等）
  - ロールは定義で宣言（型になる）、ユーザーへの割当はデータ（従業員マスタ）
  - **行レベル: 読みは全員、書きは担当者＋管理者**。バインディングの `rowFilter` に条件式DSLを再利用
  - **列レベルはv1では持たない**（認可は履歴と違って後付けできる）
  - **FEに認可ロジックを複製しない**。APIがレコードごとに `_permissions` を返す
  - **認可はプロトタイプの最初から実装する**（後付けが高くつく。導出するので実装量は小さい）
- **認証は外部IdPに委譲（OIDC優先、SAMLは客先IdPが対応しない場合のみ）。自前でパスワードを持たない**
  - **プロトタイプでは実装しない**。認証と認可の境界をモジュールとして切り、プロトタイプは開発用ユーザー詐称（`X-Dev-User`）で代替する
  - 開発用の詐称は**ビルド時に落とす**（環境変数切り替えにすると本番に残るリスク）
  - ヒアリング必要: 客先のIdP（Google Workspace / Microsoft 365 / 他）、SSOが使えるプランか、入退社時のアカウント管理
- **作らないもの**: DnDレイアウトエディタ、フォームビルダー、プラグイン機構、kintoneの機能パリティ
- **「非エンジニアが自分で作れる」を価値としない**。kintoneの中核価値をあえて捨て、表現力・コード管理・費用構造を取る
- **バックエンドは最終的に Go**。定義（TS DSL）・CLI・FE は TypeScript
  - Go習熟が浅いため、**TSでドメインモデル・制約を型とユニットテストで書き、それを仕様としてGoへ移す**
  - **TS版は仕様であり、Go版完成後は実装を捨てる**（言語非依存のテストケースだけ資産として残す）。TS実装に投資しすぎないこと
  - 移植の穴を塞ぐため、**テストケースはJSONで持ち、TS版とGo版が同じケースを流す**
  - **条件式のAST（JSON）がTSとGoの契約**。仕様として最初に固める部分
  - ⚠ **Cloudflare Workers + D1 は使えない**（WorkersはGo非対応、D1はWorkers専用）→ 提案フェーズまでに `docs/cost-simulation.md` の書き換えが必要。代替は Cloud Run + Neon/Supabase など
- **インフラは今は決めない。プロトタイプはローカルで動けばよい**（SQLiteで足りる）。**優先はドメインモデリングの実装**
  - ⚠ 条件式のSQL変換は**方言依存**。ローカルSQLite → 本番PostgreSQL で書き直しになるので、**SQL生成層は差し替え可能な形にしておく**

### 提案・ドメイン

- 提案の論拠は「コスト削減」単独にしない。安価SaaS（Zoho 1,680円/人）が存在するため、**業界特化のフィット（ヨミ管理・原稿進捗・ストック契約/解約管理）＋ユーザー数非依存の費用構造**をセットで主張する
- 金額表記は**税抜・円**で統一
- ドメインモデルは「商談（deal）」と「契約（contract）」を分離し、契約は求人広告（フロー型）/ MEO（ストック型）の商材別詳細を持つ
- 営業ドメインの設計指針（`docs/sales-domain.md`、domain-model.md 改訂で反映する）:
  - 案件の金額は**顧客請求額（総額）と自社収益（粗利）を両方持つ**。代理店ビジネスで乖離するため。予測・目標は粗利ベース。一時金と月額も分離
  - 案件の**変更履歴（金額・見込み月・status・確度）を必ず持つ**。予測精度・期ずれ・パイプライン増減は後から追加できない
  - **目標（quota）エンティティを持つ**。ダッシュボードの主役は目標達成率
  - 決着区分に「消滅（No Decision）」「保留」を独立させる。保留は予測から除外
  - ステージは売り手の作業でなく**買い手の状態変化**で定義する

## 未確認事項（ヒアリング待ち・試算やモデルに影響）

1. 契約ユーザー数・プラン・年額実額（10人未満なら2026年11月の経過措置終了＝実質値上げが提案フック）
2. 有料プラグイン利用の有無と費用（コスト試算の前提P）
3. **CRM/SFA以外のkintone利用業務**（残ると解約できず削減効果が消える。提案成否の最重要確認）
4. リード（テレアポリスト）管理をスコープに含めるか
5. 顧客の粒度（企業/店舗の分離が必要か＝チェーン店顧客の有無）
6. MEO月次レポートのデータ源（手入力か、順位計測ツールのAPIか）
7. 「フィットしない」の具体箇所、増員計画、データ量・添付ファイル利用状況
8. **案件の金額を何で管理しているか**（広告費総額 or 自社マージン。目標も同じ基準か）
9. **目標（予算）の設定単位**（個人別/チーム別、月次/四半期、商材別か）
10. テレアポ担当と営業担当が分業か（分業なら引き渡しの記録が必要）
11. ヨミの段階定義とヨミ会の運用、期ずれの扱い
12. **客先のIDプロバイダ**（Google Workspace / Microsoft 365 / 他）、SSOが使える契約プランか、入退社時のアカウント管理の運用 ← 認証をIdPに委譲するため
※ 8〜11 を含む追加ヒアリング項目の全体は `docs/sales-domain.md` §17-3

## 開発環境（2026-08-04 構築）

`../fullstack-monorepo` を参考にした Docker + TypeScript 構成。詳細は `README.md`。

- **pnpm workspace**（`packages/*`, `apps/*`）+ TypeScript 7.0.2 + `tsconfig.base.json`（strict, NodeNext）
- **vite-plus (`vp`)** で pack / test / lint / fmt を統一。テストは vitest、lint は oxlint
- パッケージ: `@alt/dsl`（条件式AST・テーブル定義・フロー定義・ロール定義・定義バンドル）、`@alt/sql`（AST→SQL変換・DDL。**Goに移植する部分**）、`@alt/definitions`（客先の定義そのもの。テーブル・営業フロー・ロール）、`@alt/cli`（`alt` コマンド）
- **`alt` の起動は `pnpm alt <cmd>`**（`tsx --tsconfig packages/cli/tsconfig.json` 経由）。**`--tsconfig` は必須** — 落とすと tsx が `paths` を見ず `dist/` の古い成果物を読む（vitest の alias と同じ罠）
- **作業はコンテナ内**: `docker compose up -d` → **`docker compose exec dev pnpm verify`**（check:wiring → fmt:check → typecheck → lint → test をまとめて実行）
  - `check:wiring` は**パッケージ追加時の「4箇所」の追記漏れ**を機械検知する（下記）。`fmt:check` が前寄りなのは、整形ミスを40秒でなく2秒で返すため
  - フェーズの着手/完了は `/phase-start <N>` / `/phase-done <N>`（`docs/implementation.md` 参照）
  - `pnpm install` は compose の command が起動時に実行する
  - **ポートはホスト側でずらしてある**: API = `3100`、FE dev サーバー = `5273`（ホストの3000/5173は別プロジェクトのコンテナが使用中のため）
  - ⚠ **コンテナを作り直すと `node_modules` が空になることがある**（匿名ボリュームだけ新しくなっても pnpm が「Already up to date」と言って再リンクしない）。`ERR_MODULE_NOT_FOUND` が出たら `docker compose exec dev sh -c 'rm -f node_modules/.pnpm-workspace-state-v1.json node_modules/.package-map.json && pnpm install'`
  - **パッケージを追加したら `docker-compose.yml` の匿名ボリュームにも追記する**
- **パッケージを追加したら4箇所**（`pnpm check:wiring` が全部を検知して落とす。手で覚えなくてよい）:
  1. `docker-compose.yml` の匿名ボリューム
  2. そのパッケージの `tsconfig.json` の `paths`（tsc用）
  3. ルート `vite.config.ts` の `resolve.alias`（vitest用）
  4. ルート `package.json` の `tsx` 起動に `--tsconfig`
  - 2〜4 はすべて「`dist/` の古い成果物を読む」という同じ壊れ方をする。**prebuild 忘れに気づけないまま通ってしまう**のが厄介な点
- **`package.json` / `pnpm-workspace.yaml` を編集したら `pnpm install` は自動で走る**（PostToolUse hook、`.claude/hooks/pnpm-install-on-manifest-change.sh`）。手で叩く必要はない。発火の証跡は `.claude/.hook-log`
- 落とし穴（対処済み）: `COREPACK_ENABLE_DOWNLOAD_PROMPT=0` が無いと、非対話起動で `pnpm install` が corepack の確認待ちで無限に止まる
- **コンテナ内で使い捨てスクリプトを実行するとき**（DBの中身を見る等）: **Write ツールでリポジトリ内の「依存を解決できるパッケージ配下」に置き**（例: `packages/cli/check.tmp.ts`）、`docker compose exec` で実行して消す。リポジトリは bind mount されているのでホストから置ける
  - scratchpad はコンテナから見えない。`/app` 直下や `/app/data` は pnpm の strict レイアウトのため依存を解決できず `ERR_MODULE_NOT_FOUND` になる（`better-sqlite3` は `packages/cli/node_modules` にしかない）
  - heredoc で `docker compose exec sh -c` に流し込まない（クォートが壊れる）
  - `git` はホスト側で叩く（コンテナ内からは「Not a git repository」になる）

## 作業ルール

- ドキュメント・コミュニケーションは日本語
- 価格・外部情報は調査日を明記（情報源は `docs/domain-research.md` の出典に追記）
- 仮置きの数値は「仮置き」と明記し、ヒアリング後に確定値へ置換する
- このCLAUDE.mdには要約と決定事項のみ。詳細は docs/ 配下へ

## 次のアクション

**方針: 優先はドメインモデリングの実装。インフラは考えない（プロトタイプはローカル + SQLite）。**

構想の設計判断は `product-concept.md` §8-1 でほぼ確定済み（未確定は §8-2 の8件で、いずれも実装しながら決まる性質のもの）。

### 完了

- `docs/domain-model.md` の改訂（v2）、条件式AST仕様（`docs/condition-ast.md`）
- 開発環境の構築（Docker + pnpm workspace + vite-plus）
- **条件式ASTの実装**（`@alt/dsl`）、テーブル定義と外部キー解決、**SQL変換**（`@alt/sql`）、実SQLiteで動く適合テスト6件
- **フェーズ1: 定義層**（2026-08-06）— プラットフォームテーブルのDDL（`_flow_state` / `_manual_check`）、フロー定義DSL、`@alt/definitions`（テーブル5本・営業フロー1本・出口条件7件）
- **フェーズ2: CLI**（2026-08-06）— `@alt/cli`。`alt validate`（3層19ルール、エラーは論理キー＋直し方つき）・`alt apply`（SQLiteにスキーマ、`--recreate` 必須の破壊的変更ガード）・`alt export`（定義バンドルをJSON）
- **フェーズ3: バックエンド**（2026-08-06）— `@alt/server`（`node:http`、フレームワーク無し）。定義レジストリ・ルート自動生成（未バインドは404）・出口条件の一括評価（一覧のクエリは件数によらず3本）・有効期間型の書き込み（閉じてINSERT、`as_of`）・ステップ遷移（未充足でも進めて `_flow_state.unmet_checks` に記録、管理者の強制遷移）・認可4層と `_permissions`・`X-Dev-User` 詐称。`alt seed` と `alt export --out` も追加

### いま進める（実装）

**→ `docs/implementation.md` を読む。それが実装セッションのハブ。**

ブラウザで動作確認できる最小スコープを4フェーズで作る。現在地は**フェーズ4（FE + 動作確認、未着手）**。
各フェーズの詳細は `docs/impl/phase-*.md` にあり、**着手するフェーズだけ読む**（完了したものと先のものは読まない）。

実装前に決めるべき設計判断はすべて決着済み:
- API は **REST 自動生成**（GraphQL/gRPC は動的スキーマと相性が悪い）
- 現在ステップは **`_flow_state` テーブル**（レコード×フローの *関係* として持つ。業務テーブルの列にしない ＝ kintone と同じ構造を避ける）
- 手動チェックは **`_manual_check`、明示キーで識別**（ラベルをキーにすると文言修正でチェックが外れる）
- フローの対象は **`flow({ target })` で明示**（`primary` バインド＝所有 とは別の軸）、起点は `initial`
- **enum の値は英語キー**。表示ラベルは `domain-model.md` §5-0 の対応表に分離
- **CLI は客先定義を静的 import する**（`packages/cli/src/bundle.ts` 1箇所に閉じる）。任意パスの実行時ロードは持たない
- **定義のルールの置き場は `alt validate` 1箇所**。定義パッケージのテストには定義集合固有の前提だけ残す
- 終端ステップの出口条件は **`next` が空なら免除**（フェーズ2で決着。保留ステップには手動チェックを足した）

未決着の要検討（`product-concept.md` §8-2 論点7・9・11・12・13）:
決着ステップと `deal.status` の二重管理（フェーズ3でも解いていない。遷移は status を書かない）/ **いま誰もマスタを更新できない**（`company`・`contact` が reference のみ。`alt seed` は開発用の裏口であって答えではない）/ 条件式AST の構文エラーの出し方 / **ステップを担当しないロール（`sales_manager`）はフローに参加できず案件を1件も読めない** / **プラットフォームが客先定義の名前（`employee` テーブル・`admin` ロール）を直に知っている**

### 客先提案（プロトタイプと並行 or 後）

5. ヒアリングシートの清書（`docs/domain-research.md` §5 + `docs/sales-domain.md` §17-3 + 上記「未確認事項」ベース）
6. `docs/cost-simulation.md` の改訂（Cloudflare Workers + D1 前提が崩れたため。Go前提のホスティング費用に置換）※提案フェーズまでに
7. ヒアリング結果でコスト試算のパラメータ（U, P, D, M）を確定値に更新
8. 提案書ドラフト（構成: 現状課題 → 選択肢比較 → 内製案 → コスト → リスクと対策 → 進め方）
   ※ 提案で基盤構想をどこまで語るかは要判断（`product-concept.md` §10-2。実験台にされる懸念を与えるリスク）
